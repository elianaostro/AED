int* sumacero(void* lista){
    /* 
    hago un hash de la lista, con key igual al valor de la lista y value el indice
    hasta que me quede el hash vacio
        si el 0 esta esta en el hash 
            devuelvo un array desde el indice, hasta la cantidade de iteraciones
        si no
            a cada elemento del hash le sumo el valor del siguiente elemento de la lista, y borro el ultimo
    devuelvo NULL
    */
}